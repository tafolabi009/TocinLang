// Recursion, type inference, and formatted output.
//   tocin examples/fib.to --run
def fib(n: int) -> int {
    if n < 2 { return n; }
    return fib(n - 1) + fib(n - 2);
}

def main() {
    for i in 0..15 {
        println("fib({}) = {}", i, fib(i));
    }
    return 0;
}
