// expect: 7
def main() -> int {
    let v = vecNew();
    vecPush(v, 1); vecPush(v, 2); vecPush(v, 99);
    vecSet(v, 1, 5);
    let top = vecPop(v);
    return (top - 99) + vecGet(v, 0) + vecGet(v, 1) + 1;
}
