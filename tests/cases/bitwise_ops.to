// expect: 109
def main() {
    let a = 12 & 10;   // 8
    let b = 12 | 10;   // 14
    let c = 12 ^ 10;   // 6
    let d = 1 << 4;    // 16
    let e = 256 >> 2;  // 64
    let f = ~0;        // -1
    return a + b + c + d + e + f + 2;  // 8+14+6+16+64-1+2 = 109
}
